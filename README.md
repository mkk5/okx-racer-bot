## Description

Simple OKX Racer automation to abuse tab switching bug and achieve ~100% winrate. Some edge cases may not be handled.

## Usage

Create the `img` folder with required images. You may also need to edit the coordinates offset of the price
areas and `fuel` variable

## Possible improvements

- AI to choose a `choice` var
- OCR to autodetect remaining fuel
